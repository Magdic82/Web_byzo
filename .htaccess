# Kanonická URL: https://www.byzo.cz
# Přesměrování non-www na www a HTTP na HTTPS

RewriteEngine On

# Přesměrování HTTP na HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Přesměrování non-www na www
RewriteCond %{HTTP_HOST} ^byzo\.cz$ [NC]
RewriteRule ^(.*)$ https://www.byzo.cz/$1 [L,R=301]

# Vlastní 404 stránka
ErrorDocument 404 /404.html
